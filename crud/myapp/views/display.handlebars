<h1>Display</h1>
{{mydata.length}} Records Found
<table border='1'>
    <tr>
        <th>Sr.no.</th>
        <th>Name</th>
        <th>Mobile</th>
        <th>Email</th>
         <th>Action</th>
    </tr>

{{#each mydata}}
    <tr>
        <td></td>
    <td>{{user_name}}</td>
    <td>{{user_mobile}}</td>
    <td>{{user_email}}</td>
    <td>
        <a href="edit/{{_id}}">Edit</a> |
        <a href="delete/{{_id}}">Delete</a>

    </td>
    </tr>
{{/each}}


</table>